spring:
  datasource:
    url: ${db-url:jdbc:postgresql://localhost:5435/moroccoexploredb}
    driver-class-name: org.postgresql.Driver
    username: ${db-username:admin}
    password: ${db-password:admin1234}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  servlet:
    context-path: /api/activity